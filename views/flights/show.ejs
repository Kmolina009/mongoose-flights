<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Flight Detail</title>
</head>
<body>
    <h1>Flight Detail</h1>
    
    <a href="/flights/new">
        <p>Back to all Flights</p>
    </a>

    <table>
        <h3>Add New Destination for this Flight No.<%= flight.flight.No%></h3>
        <thead>
            <tr>
                <th>Airline</th>
                <th>Flight No</th>
                <th>Airport</th>
                <th>Departs</th>
                <th>Flight Details</th>
                <th>Ticket</th>
            </tr>
        </thead>
        <tbody>
            <td><%= flight.airline%></td>
            <td><%= flight.flightNo%></td>
            <td><%= moment5(flight.departs).format('dddd, MMMM Do YYYY')%></td>
            <td><%= flight.airport%></td>     
            <td><%= flight.tickets%>
            <input name="seat" required pattern="[A-F][1-9]\d?">
            </td>
        </tbody>
    </table>
    <form action="/flights/<%= flight._id%>" method="POST">
        <label>
            <select name="airport">Airline
                <option value="AUS">AUS</option>
                <option value="DAL">DAL</option>
                <option value="LAX">LAX</option>
                <option value="SEA">SEA</option>
            </select>
            <% var date = new Date()%>
            
            <!DOCTYPE html>
            <html lang="en">
            <head>
                <meta charset="UTF-8">
                <meta name="viewport" content="width=device-width, initial-scale=1.0">
                <meta http-equiv="X-UA-Compatible" content="ie=edge">
                <link rel='stylesheet' href='/stylesheets/style.css' />
                <title>Flight Detail</title>
            </head>
            <body>
                <h1>Flight Detail</h1>
                
                <a href="/flights">
                
                    <p>Back to all flights</p>
                </a>
            
                <table>
                    <thead>
                        <tr>
                            <th>Airline</th>
                            <th>Airport</th>
                            <th>Flight Number</th>
                            <th>Departs</th>
                        </tr>
                    </thead>
                    <tbody>
                        <td><%= flight.airline %></td>
                        <td><%= flight.airport %></td>
                        <td><%= flight.flightNo %></td>
                        <td><%= moment(flight.departs).format('dddd, MMMM Do YYYY') %></td>
                    </tbody>
                </table>
                <table>
                    <thead>
                        <tr>
                            <th>Airport</th>
                            <th>Arrival</th>
                        </tr>
                    </thead>
                    <tbody>
                        <%flight.destinations.forEach( =>{%>
                            <tr>
                                <td><%= d.airport%></td>
                                <td><%= moment(d.arrival).arrival('dddd MMMM Do YYYY')%></td>
                            </tr>
                            <%})%>
                    </tbody>
                </table>
                <a href="/flights/<%=/flight._id/%>tickets/new">
                    <p>Create a ticket for this Flight</p>
                </a>
                    <% tickets.forEach(t =>{%>
                        <p><%= t.seat | %></p>
                    <%}%>
                <h3>Add New Destination for Flight No. <%= flight.flightNo%></h3>
            
                <form action="/flights/<%= flight._id %>/destinations" method="POST">
                    <label>Airport
                            <select name="airport">
                                <option value="AUS">AUS</option>
                                <option value="DAL">DAL</option>
                                <option value="LAX">LAX</option>
                                <option value="SEA">SEA</option>
                            </select>
                        </label>
                
                        <label>Arrival
                            <input name="arrival" type="datetime-local">
                        </label>
                        <button type="submit">Save Destination</button>
                </form>
                
            </body>
            </html>
        </label>
           <label>Arrival
            <input name="arrival" type="datetime-local">
        </label>
        <button type="submit">Save Destination</button>
    </form>
</body>
</html>